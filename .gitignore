# Internal design docs, specs & plans — kept local, not shared in the repo
docs/

# macOS
.DS_Store

# Node / frontend
node_modules/
dist/
dist-ssr/
*.local
.vite/

# Rust / Tauri (added now so build output is never committed)
src-tauri/target/
target/

# Env / secrets
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# LLM Wiki — local personal knowledge base, not shared in the repo
wiki/
raw/
CLAUDE.md

# Claude Code local config, skills & drivers — local-only, not shared in the repo
.claude/

# Editor directories
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.superpowers/

# Local code-signing cert material (see scripts/create-signing-cert.sh)
scripts/.signing/

# Verity
.verity/credentials
.verity/.cache/
.verity/.logs/
.verity/.last-analysis
.verity/.iteration-count
.verity/.last-pass-hash
.verity/.last-intent
.verity/.memory-sync-state.json
.verity/memory/log.md
.claude/settings.local.json
