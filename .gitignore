# Derived/extracted design assets — regenerate with scripts/extract-ember.py
docs/_ember_extracted/

# macOS
.DS_Store

# Node / frontend
node_modules/
dist/
dist-ssr/
*.local
.vite/

# Rust / Tauri (added now so build output is never committed)
src-tauri/target/
target/

# Env / secrets
.env
.env.*
!.env.example

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# LLM Wiki — local personal knowledge base, not shared in the repo
wiki/
raw/
CLAUDE.md

# Editor directories (allow extensions.json)
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.superpowers/

# Local code-signing cert material (see scripts/create-signing-cert.sh)
scripts/.signing/
